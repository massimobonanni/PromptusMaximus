name: Build and Release

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-test:
    name: Build, Test and Publish
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Update project versions
      id: version
      run: |
        VERSION=$(date +'%Y.%m.%d').${{ github.run_number }}
        echo "Setting version to: $VERSION"
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        
        # Update all .csproj files
        for csproj in $(find . -name "*.csproj"); do
          echo "Updating $csproj"
          if grep -q "<Version>" "$csproj"; then
            sed -i "s|<Version>.*</Version>|<Version>$VERSION</Version>|g" "$csproj"
          else
            # Add Version element if it doesn't exist
            sed -i "s|</PropertyGroup>|  <Version>$VERSION</Version>\n  </PropertyGroup>|" "$csproj" || true
          fi
        done

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Restore dependencies
      run: dotnet restore PromptusMaximus.sln

    - name: Build solution
      run: dotnet build PromptusMaximus.sln --configuration Release --no-restore

    - name: Run tests
      run: dotnet test PromptusMaximus.sln --configuration Release --no-build --verbosity normal
      continue-on-error: true

    - name: Publish PromptusMaximus.Console for Windows
      run: dotnet publish PromptusMaximus.Console/PromptusMaximus.Console.csproj --configuration Release --output ./publish-win -r win-x64 

    - name: Publish PromptusMaximus.Console for Linux
      run: dotnet publish PromptusMaximus.Console/PromptusMaximus.Console.csproj --configuration Release --output ./publish-linux -r linux-x64 

    - name: Create Windows zip archive
      run: |
        cd publish-win
        zip -r ../promptusmaximus-win-x64-${{ steps.version.outputs.version }}.zip .
        cd ..

    - name: Create Linux zip archive
      run: |
        cd publish-linux
        zip -r ../promptusmaximus-linux-x64-${{ steps.version.outputs.version }}.zip .
        cd ..

    - name: Upload Windows artifact
      uses: actions/upload-artifact@v4
      with:
        name: promptusmaximus-win-x64-${{ steps.version.outputs.version }}
        path: promptusmaximus-win-x64-${{ steps.version.outputs.version }}.zip
        retention-days: 5

    - name: Upload Linux artifact
      uses: actions/upload-artifact@v4
      with:
        name: promptusmaximus-linux-x64-${{ steps.version.outputs.version }}
        path: promptusmaximus-linux-x64-${{ steps.version.outputs.version }}.zip
        retention-days: 5

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build-and-test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download Windows artifact
      uses: actions/download-artifact@v4
      with:
        name: promptusmaximus-win-x64-${{ needs.build-and-test.outputs.version }}
        path: .

    - name: Download Linux artifact
      uses: actions/download-artifact@v4
      with:
        name: promptusmaximus-linux-x64-${{ needs.build-and-test.outputs.version }}
        path: .

    - name: Get commit info
      id: commit
      run: |
        echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
        echo "timestamp=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: release-${{ steps.commit.outputs.timestamp }}
        name: Release ${{ steps.commit.outputs.timestamp }}
        body: |
          Automated release from commit ${{ steps.commit.outputs.sha_short }}
          
          **Version:** ${{ needs.build-and-test.outputs.version }}
          
          **Platforms:**
          - Windows (x64)
          - Linux (x64)
          
          **Changes:**
          ${{ github.event.head_commit.message }}
        files: |
          promptusmaximus-win-x64-${{ needs.build-and-test.outputs.version }}.zip
          promptusmaximus-linux-x64-${{ needs.build-and-test.outputs.version }}.zip
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
